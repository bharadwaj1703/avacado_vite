{
  "name": "avacado-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run dev:all",
    "dev:all": "bun run --filter '*' dev",
    "dev:frontend": "cd frontend && bun run dev",
    "dev:backend": "cd backend && bun run dev",
    "build": "NODE_OPTIONS='--max-old-space-size=4096' bun run build:backend && NODE_OPTIONS='--max-old-space-size=4096' bun run build:frontend",
    "build:frontend": "cd frontend && NODE_OPTIONS='--max-old-space-size=4096' bun run build",
    "build:backend": "cd backend && NODE_OPTIONS='--max-old-space-size=4096' bun run build",
    "start": "bash ./start-prod.sh",
    "start:backend": "cd backend && bun run start",
    "lint": "bun run lint:frontend && bun run lint:backend",
    "lint:frontend": "cd frontend && bun run lint",
    "lint:backend": "cd backend && bun run lint",
    "migrate": "cd backend && bun run migrate"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
